
services:
  phonix_postgres:
    container_name: phonix_postgres
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    env_file: ./.env
    volumes:
      - ./data:/var/lib/postgresql/data
